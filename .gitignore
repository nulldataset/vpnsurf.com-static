# Surfer token and other temp secrets - never commit
docs/temp/

# Local inputs per site (token + site URL) - never commit
local/surfer-token
local/surfer-token.txt
local/site-url.txt

# macOS
.DS_Store

# Banner assets: only source/banner_ads is canonical; build copies to public/banner-ads
public/banner-ads/*
!public/banner-ads/.gitkeep

# Local mirror of live site (wget) — optional, do not commit
live-snapshot/
