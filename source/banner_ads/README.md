# Banner ads

Banner ad assets (728×90 and other sizes) live here. The build copies all of them to `public/banner-ads/`. Keep or replace with your own; the injector uses one 728×90 for top/bottom by default (see [docs/BANNER_ADS.md](../../docs/BANNER_ADS.md) for placement and script details).
